<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.waiterapp">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WaiterApp">
        <activity
            android:name=".activity.grouping.GroupingActivity"
            android:exported="true" />
        <activity
            android:name=".activity.customer.AddEditCostomerActivity"
            android:exported="true" />
        <activity
            android:name=".activity.customer.CustomerActivity"
            android:exported="true" />
        <activity
            android:name=".activity.homepage.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".activity.login.login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>